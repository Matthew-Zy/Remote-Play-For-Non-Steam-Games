[package]
name = "Remote-Play-For-Non-Steam-Games"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "remote-play"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[build-dependencies]
winres = "0.1"
